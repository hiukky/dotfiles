[user]
{{- if .signingKey }}
	signingkey = {{ .signingKey }}
{{- end }}
	name = {{ .name }}
	email = {{ .email }}
[commit]
	gpgsign = {{ if .signingKey }}true{{ else }}false{{ end }}
[core]
	editor = vim
[gpg]
	program = gpg
